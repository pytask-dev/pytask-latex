name: pytask-latex

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python >= 3.7
  - pip
  - setuptools_scm
  - toml

  # Package dependencies
  - pytask >= 0.3
  - pytask-parallel >= 0.3
  - latex-dependency-scanner >=0.1.1
  - pybaum >=0.1.1

  # Misc
  - black
  - jupyterlab
  - matplotlib
  - pre-commit
  - pytest
  - pytest-cov
  - pytest-xdist

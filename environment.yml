name: pytask-latex

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python >= 3.7
  - pip
  - setuptools_scm
  - toml

  # Package dependencies
  - pytask >= 0.1.0
  - pytask-parallel >= 0.1.0
  - latex-dependency-scanner

  # Misc
  - black
  - jupyterlab
  - matplotlib
  - pre-commit
  - pytest
  - pytest-cov
  - pytest-xdist
